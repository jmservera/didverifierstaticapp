# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2


      - name: Azure Static Web Apps Deploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          # Action to perform
          action: upload
          azure_static_web_apps_api_token:  674a96992cc37b19770b43cfe9af16ba5ebf8ec9bc95064881b1605e1ac13320-565cc0ae-9ca6-48d0-97b3-8325e6965dfc00327414
          # Directory location of the application source code
          app_location: /html
          # Identical in use to output_location
          app_artifact_location: /html
          # Skips the build step for the application source code if set to true.
          skip_app_build: true
